{
  "scripts": {
    "build": "pnpm --filter lo-que-pida-frontend build",
    "dev": "pnpm --recursive dev",
    "start": "pnpm --filter lo-que-pida-frontend start",
    "build:all": "pnpm --recursive build"
  },
  "dependencies": {
    "@apollo/client": "^3.13.9",
    "graphql": "^16.11.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.12",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.12"
  }
}
