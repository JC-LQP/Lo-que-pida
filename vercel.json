{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/next",
      "config": {
        "distDir": ".next"
      }
    }
  ],
  "installCommand": "cd frontend && npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
